<Page
    x:Class="GUI2.Viewers.Resource"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GUI2.Viewers"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <StackPanel VerticalAlignment="Top" >
        <NavigationView x:Name="navView" PaneDisplayMode="Top" SelectionChanged="OnSelectionChanged" MenuItemsSource="{x:Bind Tabs}">
            <NavigationView.MenuItemTemplate>
                <DataTemplate x:DataType="local:ResourceTab">
                    <NavigationViewItem Content="{x:Bind DisplayName}" />
                </DataTemplate>
            </NavigationView.MenuItemTemplate>
        </NavigationView>
        <Frame x:Name="contentFrame"  />
    </StackPanel>
</Page>
